import { ConnectionRequest } from "./connectedAccounts";
import { Actions } from "./actions";
import { Apps } from "./apps";
import { Integrations } from "./integrations";
import { ActiveTriggers } from "./activeTriggers";
import { ConnectedAccounts } from "./connectedAccounts";
import { ExecuteActionResDTO } from "../client";
import { BackendClient } from "./backendClient";
import { Triggers } from "./triggers";
export declare class Entity {
    id: string;
    backendClient: BackendClient;
    triggerModel: Triggers;
    actionsModel: Actions;
    apps: Apps;
    connectedAccounts: ConnectedAccounts;
    integrations: Integrations;
    activeTriggers: ActiveTriggers;
    constructor(backendClient: BackendClient, id?: string);
    execute(actionName: string, params?: Record<string, any> | undefined, text?: string | undefined, connectedAccountId?: string): Promise<ExecuteActionResDTO>;
    getConnection(app?: string, connectedAccountId?: string): Promise<any | null>;
    setupTrigger(app: string, triggerName: string, config: {
        [key: string]: any;
    }): Promise<any>;
    disableTrigger(triggerId: string): Promise<any>;
    getConnections(): Promise<import("../client").ConnectionParams[]>;
    getActiveTriggers(): Promise<any>;
    initiateConnection(appName: string, authMode?: any, authConfig?: {
        [key: string]: any;
    }, redirectUrl?: string, integrationId?: string): Promise<ConnectionRequest>;
}
