"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const globals_1 = require("@jest/globals");
const getBackendClient_1 = require("../testUtils/getBackendClient");
const activeTriggers_1 = require("./activeTriggers");
(0, globals_1.describe)("Apps class tests", () => {
    let backendClient;
    let activeTriggers;
    (0, globals_1.beforeAll)(() => {
        backendClient = (0, getBackendClient_1.getBackendClient)();
        activeTriggers = new activeTriggers_1.ActiveTriggers(backendClient);
    });
    (0, globals_1.it)("should get a specific action", async () => {
        const activeTriggersList = await activeTriggers.list();
        (0, globals_1.expect)(activeTriggersList).toBeInstanceOf(Array);
        (0, globals_1.expect)(activeTriggersList).not.toHaveLength(0);
    });
    (0, globals_1.it)("should get a list of actions", async () => {
        const activeTriggersList = await activeTriggers.list();
        const firstTrigger = activeTriggersList[0];
        const activeTrigger = await activeTriggers.get({ path: {
                triggerId: firstTrigger.id
            }
        });
        (0, globals_1.expect)(activeTrigger).toBeDefined();
    });
});
//# sourceMappingURL=activeTriggers.spec.js.map