import { Command } from "commander";
import { GetConnectorListResDTO } from "../sdk/client";
export default class AddCommand {
    private program;
    constructor(program: Command);
    private handleAction;
    shouldForceConnectionSetup(): Promise<void>;
    private waitUntilConnected;
    private setupConnections;
    private createIntegration;
    collectInputFields(fields: {
        name: string;
        displayName: string;
        display_name: string;
        expected_from_customer: boolean;
        required: boolean;
        type: string;
    }[], isConnection?: boolean): Promise<Record<string, any>>;
    setupIntegration(app: any, authMode: any, useComposioAuth: boolean, config: Record<string, any>): Promise<GetConnectorListResDTO | undefined>;
}
