"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const chalk_1 = __importDefault(require("chalk"));
const config_1 = require("../sdk/utils/config");
const client_1 = __importDefault(require("../sdk/client/client"));
// @ts-ignore
const resolve_package_path_1 = __importDefault(require("resolve-package-path"));
const fs_1 = __importDefault(require("fs"));
const path_1 = __importDefault(require("path"));
class AppsCommand {
    constructor(program) {
        this.program = program;
        const command = this.program.command("apps");
        command
            .description("List all apps you have access to")
            .action(this.handleAction.bind(this));
        new AppUpdateCommand(command);
    }
    async handleAction(options) {
        (0, config_1.getAPISDK)();
        const { data, error } = await client_1.default.apps.getApps({});
        if (!!error) {
            console.log(chalk_1.default.red(error.message));
            return;
        }
        console.log("Here are the apps you have access to:");
        for (const app of (data === null || data === void 0 ? void 0 : data.items) || []) {
            console.log(app.key);
        }
    }
}
exports.default = AppsCommand;
class AppUpdateCommand {
    constructor(program) {
        this.program = program;
        this.program
            .command("update")
            .description("Update apps")
            .action(this.handleAction.bind(this));
    }
    async updateActionsAndAppList(appList, actionsList) {
        try {
            const constantPath = (0, resolve_package_path_1.default)("composio-core", process.cwd());
            let constantFilePath = "";
            try {
                const fileNamePath = process.argv[1];
                // if ts-node is used then we need to update the constants file in the root folder
                // this will only work for the build
                if (fileNamePath.includes("cli/index.ts")) {
                    constantFilePath = path_1.default.join("/Users/himanshu/Desktop/composio/composio/js", "./lib/src/constants.js");
                }
                else {
                    // if package is used then we need to update the constants file in the package folder
                    constantFilePath = path_1.default.join(constantPath, "../lib/src/constants.js");
                }
            }
            catch (e) {
                console.log(chalk_1.default.red("Error while updating constants file"));
                console.log(chalk_1.default.red(e.message));
            }
            const constantFile = fs_1.default.readFileSync(constantFilePath, "utf8");
            const updatedConstantFile = constantFile
                .replace(/\/\/ apps list start here[\s\S]*?\/\/ apps list end here/, `// apps list start here\n${appList}// apps list end here`)
                .replace(/\/\/ actions list start here[\s\S]*?\/\/ actions list end here/, `// actions list start here\n    ${actionsList}\n    // actions list end here`);
            fs_1.default.writeFileSync(constantFilePath, updatedConstantFile);
            console.log(chalk_1.default.green("Constants file updated successfully"), chalk_1.default.green(constantFilePath));
        }
        catch (e) {
            console.log(chalk_1.default.red("Error while updating constants file"));
            console.log(chalk_1.default.red(e.message));
        }
    }
    async handleAction() {
        (0, config_1.getAPISDK)();
        const appList = await client_1.default.apps
            .getApps({})
            .then((res) => {
            var _a;
            return ((_a = res.data) === null || _a === void 0 ? void 0 : _a.items.map((app) => `'${app.key.toUpperCase()}': '${app.key}'`).join(",\n")) || [];
        });
        // @ts-ignore
        const actionsList = await client_1.default.actionsV2
            .v2ListActions({})
            .then((res) => {
            var _a;
            return ((_a = res.data) === null || _a === void 0 ? void 0 : _a.items.map((action) => `'${action.enum}': '${action.enum}'`).join(",\n")) || [];
        });
        await this.updateActionsAndAppList(appList, actionsList);
    }
}
//# sourceMappingURL=apps.js.map