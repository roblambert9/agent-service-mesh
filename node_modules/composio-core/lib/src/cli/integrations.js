"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const chalk_1 = __importDefault(require("chalk"));
const client_1 = __importDefault(require("../sdk/client/client"));
const config_1 = require("../sdk/utils/config");
const util_1 = require("./src/util");
class ConnectionsCommand {
    constructor(program) {
        this.program = program;
        const command = this.program.command("integrations");
        command
            .description("List all integrations you have created or connected")
            .option("-a, --active", "Show only active integrations")
            .action(this.handleAction.bind(this));
    }
    async handleAction(options) {
        (0, config_1.getAPISDK)();
        const { data, error } = await client_1.default.appConnector.listGlobalConnectors({
            query: options.active ? { status: "ACTIVE" } : {},
            throwOnError: false,
        });
        if (error) {
            console.log(chalk_1.default.red(error.message));
            return;
        }
        for (const integration of (data === null || data === void 0 ? void 0 : data.items) || []) {
            const typedIntegration = integration;
            console.log(chalk_1.default.cyan(`• ${chalk_1.default.bold("Id")}: ${typedIntegration.id}`));
            console.log(chalk_1.default.magenta(`  ${chalk_1.default.bold("App")}: ${typedIntegration.appName}`));
            console.log(chalk_1.default.magenta(`  ${chalk_1.default.bold("Created At")}: ${(0, util_1.parseDate)(typedIntegration.createdAt)}`));
            console.log(""); // Add an empty line for better readability between connections
        }
    }
}
exports.default = ConnectionsCommand;
//# sourceMappingURL=integrations.js.map