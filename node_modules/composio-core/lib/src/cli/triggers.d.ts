import { Command } from "commander";
export default class ConnectionsCommand {
    private program;
    constructor(program: Command);
    private handleAction;
}
export declare class TriggerAdd {
    private program;
    constructor(program: Command);
    handleAction(triggerName: string): Promise<void>;
}
export declare class TriggerDisable {
    private program;
    constructor(program: Command);
    handleAction(triggerId: string): Promise<void>;
}
export declare class ActiveTriggers {
    private program;
    constructor(program: Command);
    handleAction(): Promise<void>;
}
