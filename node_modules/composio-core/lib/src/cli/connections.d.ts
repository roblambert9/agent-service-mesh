import { Command } from "commander";
export default class ConnectionsCommand {
    private program;
    constructor(program: Command);
    private handleAction;
}
export declare class ConnectionsGetCommand {
    private program;
    constructor(program: Command);
    private handleAction;
}
