import { Workspace } from "./base";
import { WorkspaceConfig } from "./config";
export declare enum ExecEnv {
    HOST = "HOST",
    DOCKER = "DOCKER",
    E2B = "E2B"
}
export declare class WorkspaceFactory {
    workspace: Workspace | null;
    id: string | null;
    env: ExecEnv;
    workspaceConfig: WorkspaceConfig;
    constructor(env: ExecEnv, kwargs: WorkspaceConfig);
    new(): Promise<void>;
    get(id?: string | null): Promise<Workspace>;
    close(): Promise<void>;
}
